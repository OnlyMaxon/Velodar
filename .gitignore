# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native builds (bare workflow — generated by `expo prebuild` / run)
/ios
/android

# Environment / secrets
.env
.env.local
*.keystore
!debug.keystore

# Metro
.metro-health-check*

# Local AI onboarding / tooling — kept out of VCS by preference
CLAUDE.md
.claude/settings.local.json

# macOS / editors
.DS_Store
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
.idea/
.vscode/
